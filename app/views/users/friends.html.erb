<div class="my-4 has-text-centered">
  <h2 class="title"><%= @user.full_name %>'s Friends(<%= @friends.length %>)</h2>
  <%= link_to 'Back to Profile', @user, class: 'button is-primary' %>
</div>
<%= turbo_frame_tag @user do %>
  <article class="columns is-variable is-multiline is-8">
    <% 20.times.each do |_| %>
      <div class="column is-flex-grow-0 box has-text-centered">
        <%= link_to @friends.first do %>
          <figure class="image is-128x128">
            <%= image_tag @friends.first.photo, class: 'image-128x128  is-rounded' %>
          </figure>
          <p class="title is-4 mt-2"><%= @friends.first.full_name %></p>
        <% end %>
      </div>
    <% end %>
  </article>
<% end %>
