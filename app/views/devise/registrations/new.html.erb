<div class="ml-5">
  <h2 class="title">Sign up</h2>

  <%= form_for(resource, as: resource_name, url: registration_path(resource_name), class: 'ml-4') do |f| %>
    <%= render "devise/shared/error_messages", resource: resource %>

    <div class="field py-2">
      <%= f.label :email, class: "is-size-5 has-text-weight-bold" %>
      <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "input" %>
    </div>

    <div class="field py-2 columns">
      <div class="column">
        <%= f.label :first_name, class: "is-size-5 has-text-weight-bold" %>
        <%= f.text_field :first_name, autocomplete: "first_name", class: "input" %>
      </div>
      <div class="column">
        <%= f.label :last_name, class: "is-size-5 has-text-weight-bold" %>
        <%= f.text_field :last_name, autocomplete: "last_name", class: "input" %>
      </div>
    </div>

    <div class="field py-2">
      <%= f.label :birthday, class: "is-size-5 has-text-weight-bold" %><br />
      <%= f.date_field :birthday %>
    </div>

    <div class="field py-2">
      <%= f.label :gender, 'Gender (optional)', class: "gender-label is-size-5 has-text-weight-bold" %><br />
      <div class="select">
        <%= f.select :gender, ['Male', 'Female', 'Custom', '' ] %>
      </div>
    </div>

    <div class="field py-2">
      <%= f.label :password, class: "is-size-5 has-text-weight-bold" %>
      <% if @minimum_password_length %>
      <em>(<%= @minimum_password_length %> characters minimum)</em>
      <% end %><br />
      <%= f.password_field :password, autocomplete: "new-password", class: "input" %>
    </div>

    <div class="field py-2">
      <%= f.label :password_confirmation, class: "is-size-5 has-text-weight-bold" %>
      <%= f.password_field :password_confirmation, autocomplete: "new-password", class: "input" %>
    </div>

    <div class="actions">
      <%= f.submit "Sign up", class: "button is-primary" %>
    </div>
  <% end %>
</div>
